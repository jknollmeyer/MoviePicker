#include "MoviePicker.h"
#include "userinput.h"

using namespace std;

int main(){
    
    initialfileopen();
    
    settings settingspane;
    
    int num_watchers;
    
    vector<userinput> watchers (num_watchers);
    
    for (int i= 0; i < num_watchers; i++){
        watchers[i] = userinput();
    }
    
    // Just barely started
    
}
